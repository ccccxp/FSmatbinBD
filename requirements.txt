# FSMatbinBD 依赖包
# 用于 pip install -r requirements.txt

# Qt GUI 框架
PySide6>=6.5.0

# 打包工具
pyinstaller>=6.0.0

# 图标转换（PyInstaller 需要将 PNG 转换为 ICO）
Pillow>=10.0.0

# 数据库
# SQLite 为 Python 内置，无需额外安装

# 可选：性能优化
# rapidfuzz>=3.0.0  # 可选：更快的字符串匹配
